---

---

<footer>
  <div class="wrap">
    <div class="fbrand">BEERPONG <b>IV</b></div>
    <div class="fmeta">6 AGO 2026 · CABRA DEL SANTO CRISTO · POLIDEPORTIVO · 18:00 → CIERRE</div>
  </div>
</footer>

<style>
  footer {
    padding: 48px 0;
    border-top: 1px solid var(--line);
    background: var(--night);
  }

  footer .wrap {
    display: flex;
    flex-wrap: wrap;
    gap: 16px;
    align-items: center;
    justify-content: space-between;
  }

  .fbrand {
    font-family: var(--display);
    font-size: 1.4rem;
  }

  .fbrand b {
    color: var(--cup);
  }

  .fmeta {
    font-family: var(--mono);
    font-size: 0.76rem;
    letter-spacing: 0.1em;
    color: var(--muted);
  }
</style>
